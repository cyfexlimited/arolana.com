[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[start]
cmd = "gunicorn arolana_config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4 --worker-class gthread --timeout 90 --keep-alive 5 --access-logfile - --error-logfile -"
