from django.apps import AppConfig


class InstallersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "installers"
    verbose_name = "Verified Installers & Engineers"

