{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --noinput"
  },
  "deploy": {
    "preDeployCommand": "python manage.py migrate --noinput && python manage.py setup_google_oauth --from-env",
    "startCommand": "python manage.py migrate --noinput && python manage.py setup_google_oauth --from-env && daphne -b 0.0.0.0 -p $PORT arolana_config.asgi:application",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/health/"
  }
}
